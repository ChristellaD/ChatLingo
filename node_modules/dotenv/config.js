
// config.js

// Configuration settings for your application

const config = {
  // Database configuration
  database: {
    host: 'localhost',
    port: 27017,
    dbName: 'my_database',
    // Add other database configurations as needed
  },

  // Server configuration
  server: {
    port: 3000,
    // Add other server configurations as needed
  },

  // API configuration
  api: {
    baseURL: 'http://api.example.com',
    apiKey: 'YOUR_API_KEY',
    // Add other API configurations as needed
  },
};

module.exports = config;


// index.js

const  = require('./config');

console.log(config.database.host); // Output: localhost
console.log(config.server.port); // Output: 3000
console.log(config.api.baseURL); // Output: http://api.example.com



